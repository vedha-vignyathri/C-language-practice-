
#include <stdio.h>
int main() {
    int n, rem, sum=0, orig;
    scanf("%d", &n);
    orig = n;
    while(n != 0) {
        rem = n % 10;
        sum += rem*rem*rem;
        n /= 10;
    }
    if(sum == orig) printf("Armstrong");
    else printf("Not Armstrong");
    return 0;
}
